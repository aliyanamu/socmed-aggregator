const express = require('express'),
    router = express.Router(),
    {  } = require('../controllers/users');

/* GET users listing. */
router
    // .get('/', echo)
    
    // .post('/', insert)

    // .put('/:id', update)

    // .delete('/:id', remove)

module.exports = router;